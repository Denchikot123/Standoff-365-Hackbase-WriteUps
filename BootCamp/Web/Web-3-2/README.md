# Отчет по прохождению Web-3-2

## 📝 Общая информация
* **Категория:** BootCamp
* **Уровень сложности:** Beginner
* **Условие:**

Получите RCE на узле utils.edu.stf (10.124.1.237).

Для получения флага выполните скрипт /home/rceflag.

---

## 🔍 Стадия 1: Разведка (Reconnaissance)

### 1. Сканирование портов (Nmap)
```bash
nmap -sV -sC -p- 10.10.10.X
```
*Результаты сканирования:*
*   Порт `80/tcp` — HTTP (Apache 2.4.41)
*   Порт `22/tcp` — SSH (OpenSSH 8.2p1)

### 2. Веб-разведка / Перебор директорий
*   При переходе на сайт обнаружена форма авторизации.
*   Запущен перебор директорий с помощью `gobuster`/`ffuf`:
```bash
ffuf -w wordlist.txt -u http://10.10.10.X/FUZZ
```
*   Найден скрытый путь: `/secret-panel/`

---

## ⚡ Стадия 2: Эксплуатация (Exploitation)

### 1. Поиск уязвимости
*   В компоненте `X` была обнаружена уязвимость [Название CVE или тип уязвимости, например, SQLi / LFI].
*   Уязвимый параметр: `?page=`

### 2. Получение первичного доступа (Foothold)
*   [Краткое описание шагов для получения доступа]
*   Использован эксплоит для получения Reverse Shell.
*   Команда для подключения:
```bash
nc -lvnp 4444
```

---

## 🏁 Флаги (Flags)
*   **User.txt:** `FLAG{user_flag_here}`
*   **Root.txt:** `FLAG{root_flag_here}`
